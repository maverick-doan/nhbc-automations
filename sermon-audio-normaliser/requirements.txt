# Requirements for sermon-audio-normaliser
tzdata
# Requires Python 3.10 or newer (uses PEP 604 union types).
# External dependency: FFmpeg must be installed and available on PATH and OneDrive must be installed and configured on the system.
# Download: https://ffmpeg.org/download.html
